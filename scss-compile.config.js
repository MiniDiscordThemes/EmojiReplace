/** @type {import('bd-scss/lib/config').Config} */
export default {
    meta: {
        name: "EmojiReplace" /* The official name of your theme. */,
        scss: "Apple" /* The name of your base scss file. */,
        repo: "EmojiReplace" /* The name of your theme repository. */,
        version: "dev" /* The version of your theme. */
    }
};
